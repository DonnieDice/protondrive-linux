# verify: install + smoke-test on the matching VM via the per-distro script.
verify:ubuntu-24.04:
  extends: .verify:base
  needs: ["build:deb:ubuntu-24.04"]
  script:
    - bash scripts/ci/deploy/ubuntu-24.04/deploy.sh
