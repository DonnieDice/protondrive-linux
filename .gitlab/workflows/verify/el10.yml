# verify: install + smoke-test on the matching VM via the per-distro script.
verify:rpm-el10:
  extends: .verify:base
  needs: ["build:rpm:el10"]
  script:
    - bash scripts/ci/deploy/el10/deploy.sh
