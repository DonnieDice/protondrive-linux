# verify: install + smoke-test the fedora43 package on its VM.
verify:rpm-fedora-43:
  extends: .verify:base
  needs: ["build:rpm:fedora-43"]
  script:
    - bash scripts/ci/deploy-and-test-vm.sh fedora43
