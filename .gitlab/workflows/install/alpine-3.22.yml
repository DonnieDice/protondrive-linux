install:alpine-3.22:
  extends: .install:base
  needs:
    - job: "transfer:alpine-3.22"
      artifacts: true
  artifacts:
    name: "install-results-alpine-3.22"
    paths:
      - transfer-results/
      - install-results/
    when: always
    expire_in: 30 days
  script:
    - bash scripts/ci/install/alpine-3.22/install.sh
