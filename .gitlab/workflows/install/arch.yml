install:aur:
  extends: .install:base
  needs:
    - job: "transfer:aur"
      artifacts: true
  artifacts:
    name: "install-results-aur"
    paths:
      - transfer-results/
      - install-results/
    when: always
    expire_in: 30 days
  script:
    - bash scripts/ci/install/arch/install.sh
