transfer:debian-12:
  extends: .transfer:base
  needs:
    - job: "build:deb:debian-12"
      artifacts: true
  artifacts:
    name: "transfer-results-debian-12"
    paths: [transfer-results/]
    reports:
      dotenv: transfer-results/debian12.env
    when: always
    expire_in: 30 days
  script:
    - bash scripts/ci/transfer/debian-12/transfer.sh
