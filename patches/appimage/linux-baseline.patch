diff --git a/src-tauri/src/main.rs b/src-tauri/src/main.rs
index 246078bb..cb133e17 100644
--- a/src-tauri/src/main.rs
+++ b/src-tauri/src/main.rs
@@ -320,11 +320,14 @@ fn sanitize_url_for_log(url: &str) -> String {
 }
 
 fn main() {
-    // Fix WebKitGTK EGL/GPU issues on various Linux configurations
+    // WebKitGTK environment for Linux baseline (glibc 2.35+, webkit2gtk 2.46+).
+    // AppImage is host-distro portable; compatibility boundary is glibc age.
+    // GDK_GL=software is required; GDK_GL=disable can SIGABRT on heavy re-renders.
     std::env::set_var("WEBKIT_DISABLE_DMABUF_RENDERER", "1");
     std::env::set_var("WEBKIT_DISABLE_COMPOSITING_MODE", "1");
-    std::env::set_var("WEBKIT_FORCE_SANDBOX", "0");
-    std::env::set_var("GDK_GL", "disable");
+    std::env::set_var("WEBKIT_DISABLE_SANDBOX_THIS_IS_DANGEROUS", "1");
+    std::env::set_var("JSC_useWasmIPInt", "false");
+    std::env::set_var("GDK_GL", "software");
     std::env::set_var("GSK_RENDERER", "cairo");
 
     // Create shared client with cookie jar
